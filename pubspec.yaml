name: saju_quiz
description: Saju Master Learning App
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  font_awesome_flutter: ^10.5.0
  provider: ^6.0.0
  go_router: ^13.0.0
  flutter_svg: ^2.0.9
  firebase_core: ^26.0.0
  firebase_auth: ^4.14.0
  cloud_firestore: ^4.13.0
  intl: ^0.19.0
  purchases_flutter: ^7.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
